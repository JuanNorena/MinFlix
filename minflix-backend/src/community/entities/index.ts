/**
 * Índice de exportación de entidades del módulo de comunidad.
 *
 * Centraliza las exportaciones de `FavoriteEntity`, `RatingEntity` y `ReportEntity`.
 */

/** Entidad de favoritos (tabla `FAVORITOS`) */
export { FavoriteEntity } from './favorite.entity';

/** Entidad de calificaciones (tabla `CALIFICACIONES`) */
export { RatingEntity } from './rating.entity';

/** Entidad de reportes (tabla `REPORTES`) */
export { ReportEntity } from './report.entity';
